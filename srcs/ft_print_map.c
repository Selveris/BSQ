#include "ft_map.h"
#include "ft_string.h"

int	ft_write_map(int fd, t_map *map)
{	
	int w;
	int h;

	return (0);
}

int	ft_write_ext_map(int fd, t_map *map)
{
	return (0);
}

int	ft_write_line(int fd, char *line, int size)
{
	return (0);
}
